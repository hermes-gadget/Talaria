<?xml version="1.0" encoding="utf-8"?>
<!-- Render the scoped cache immediately; the receiver queues a constrained
     WorkManager refresh and HermesSyncWorker updates the widget after sync. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_status_label"
    android:initialLayout="@layout/widget_status_loading"
    android:minWidth="180dp"
    android:minHeight="80dp"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
