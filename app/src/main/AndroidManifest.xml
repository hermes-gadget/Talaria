<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- A-35: the app degrades gracefully without a microphone (voice features
         are optional), so don't let Play filter out mic-less devices. -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:name=".TalariaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="PictureInPictureIssue"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Talaria"
        tools:targetApi="37">

        <!-- Car app library requirement: min template API level. MUST be on
             <application> — AppInfo.retrieveMinCarAppApiLevel reads it via
             getApplicationInfo(GET_META_DATA); on the service it's invisible
             and the host throws "Min API level not declared in manifest".
             ConversationItem requires 7. -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="7" />

        <!-- Android Auto (phone projection) discovery: capability descriptor. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.Talaria"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="talaria" />
                <data android:host="chat" />
                <data android:host="connect" />
                <data android:host="status" />
                <data android:host="activity" />
                <data android:host="manage" />
                <data android:host="voice" />
            </intent-filter>
            <!-- Scoped links are delivered by explicit app-owned notification
                 PendingIntents. Keep them out of implicit/browsable resolution
                 so another app or browser cannot inject a connection/profile
                 scope through a talaria:// URL. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <data android:scheme="talaria" />
                <data android:host="session" />
                <data android:host="pairing" />
            </intent-filter>
        </activity>

        <!-- External shares are staged in an owned, bounded cache before the
             user chooses a profile-scoped chat target. Keep this surface
             separate from the main navigation activity so a share never
             bypasses the capture/draft/validation flow. -->
        <activity
            android:name=".feature.capture.ShareCaptureActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Talaria"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Android Automotive OS entry point: on head units the car
             launcher starts THIS activity, which renders the templated car
             UI via the template host. Declared WITHOUT a launcher intent
             filter — MainActivity detects automotive and hands off here, so
             phone and car stay one APK. Phone-projected Android Auto never
             starts it (CarAppService is the entry there). -->
        <activity
            android:name="androidx.car.app.activity.CarAppActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>

        <!-- PIP entry is policy-gated (pipModeState.shouldEnterOnUserLeave);
             setAutoEnterEnabled(true) would bypass that gate, so the manual
             onUserLeaveHint path stays. -->
        <activity
            android:name=".feature.pip.PipChatActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:exported="false"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.Talaria"
            tools:ignore="PictureInPictureIssue" />

        <receiver
            android:name=".worker.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".core.notifications.NotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".widget.TalariaStatusWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_status_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/status_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.TalariaQuickEntryWidgetReceiver"
            android:exported="true"
            android:label="Talaria quick entry">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_entry_widget_info" />
        </receiver>

        <service
            android:name=".core.notifications.AgentTaskNotificationService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!-- Android Auto / Automotive OS car app (sideloaded, MESSAGING category). -->
        <!-- CarAppService MUST be exported for the AA host to bind it;
             caller gating is CarHostSecurity (enrolled certs + HostValidator). -->
        <service
            android:name=".car.TalariaCarService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.MESSAGING" />
            </intent-filter>
        </service>

        <service
            android:name=".widget.TalariaTileService"
            android:exported="true"
            android:icon="@drawable/ic_talaria"
            android:label="@string/qs_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
