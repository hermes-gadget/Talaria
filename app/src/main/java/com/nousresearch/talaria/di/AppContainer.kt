package com.nousresearch.talaria.di

import android.content.Context

/** Scaffold DI graph; expanded as feature slices land. */
class AppContainer(context: Context) {
    private val appContext = context.applicationContext
}
